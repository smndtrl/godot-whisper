name: Build Android
on:
  workflow_dispatch:
  workflow_call:

env:
  GODOT_BASE_BRANCH: main

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - precision: single
            arch: arm64
          - precision: single
            arch: arm32
          - precision: single
            arch: x86_64
          - precision: single
            arch: x86_32

          - precision: double
            arch: arm64
          - precision: double
            arch: arm32
          - precision: double
            arch: x86_64
          - precision: double
            arch: x86_32
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: 🔗 GDExtension Build
        uses: ./.github/actions/scons-build
        with:
          platform: android
          arch: ${{ matrix.arch }}
          float-precision: ${{ matrix.precision }}
          build-target-type: template_release

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Godot_Whisper-android-${{ matrix.arch }}-${{ matrix.precision }}
          path: |
            ${{ github.workspace }}/bin/**/*.so
          if-no-files-found: error
